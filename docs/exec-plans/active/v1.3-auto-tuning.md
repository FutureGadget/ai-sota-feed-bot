# v1.3 Execution Plan — Auto-Tuning + Strict Mix

## Goals
1. Auto-adjust source weights using accumulated feedback. ✅ (2026-06-11)
2. Enforce stricter digest type mix with minimum per-type guarantees when available.

## Deliverables
- `pipeline/auto_tune.py` ✅ — `report` / `apply` / `sync-ctr`; blends explicit
  feedback with click-through rate (clicks from PostHog, rank-weighted exposure
  from run snapshots); guardrails in `config/ranking.yaml` `auto_tune:`
- Ranking integration ✅ — `source_tune` delta applied in
  `pipeline/ranking.py` stage C next to `source_bias`, with staleness cutoff
- Daily workflow ✅ — `.github/workflows/feedback-sync.yml` runs
  feedback sync → CTR sync → `auto_tune.py apply` → runtime commit; the next
  pipeline run picks up `data/feedback/source_adjustments.json`
- Ops observability ✅ — `pipeline/ops_daily_summary.py` reports
  `source_tuning` (generated_at, adjusted source count, top deltas)
- `config/profile.yaml` diversity `min_per_type` (pending)
- `pipeline/build_digest.py` strict minimum mix logic (pending)

## Validation
- `python pipeline/auto_tune.py report`
- `python pipeline/auto_tune.py apply`
- `python collectors/collect.py && python pipeline/build_digest.py`
