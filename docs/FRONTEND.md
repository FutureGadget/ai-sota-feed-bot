# FRONTEND.md

Current product is backend/workflow-first (no standalone frontend yet).
Primary user surface is Telegram + GitHub Issue digest.

Future frontend options:
- Minimal web dashboard for ranked items/history
- Feedback UI for tuning relevance weights
