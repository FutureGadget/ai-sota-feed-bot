feedparser==6.0.11
PyYAML==6.0.2
python-dateutil==2.9.0.post0
requests==2.32.3
# Optional at runtime: per-edition OG share cards (pipeline/og_cards.py).
# Everything degrades gracefully where it's absent (Vercel build, agent routines).
pillow==12.3.0
